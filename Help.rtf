{\rtf1\ansi\deff0{\fonttbl{\f0 Segoe UI;}}
\fs20
{\b\fs28 AC-MXNET USP-CBOX Control \line}
{\fs18 Driver v3.9 \'96 AVPro Global IPCBox (USP API) \line}
\line
This driver controls an AVPro AC-MXNET USP-CBOX over TCP (default port 24). It supports Multiview, Matrix routing, Video Wall recall, the Media Player, CEC display control, and per-device / system control, with two-way feedback exposed as System Variables.\line
\line
{\b SETUP \line}
1. In {\b Connection Settings} enter the CBOX {\b IP Address} and {\b USP Port} (24).\line
2. Populate the definition slots with the IDs/names the CBOX already knows:\line
\tab {\b Input (Source) Definitions} I1-I20: encoder (TX) IDs or MAC addresses.\line
\tab {\b Output (Display) Definitions} O1-O10: decoder (RX) IDs or MAC addresses.\line
\tab {\b Layout Definitions} L1-L10: Multiview layout names (mvid).\line
\tab {\b Matrix Definitions} MX1-MX4: saved matrix preset names.\line
\tab {\b Video Wall Definitions} VW1-VW4: video wall names.\line
\tab {\b Video Wall Layout Definitions} WL1-WL8: wall layout names.\line
\tab {\b Playlist Definitions} PL1-PL8: media playlist names.\line
\tab {\b CEC Command Definitions} C1-C8: CEC payloads (see CEC below).\line
The drop-down lists in System Functions show these slot values by name.\line
\line
{\b SYSTEM FUNCTIONS \line}
{\b Multiview:} Set Layout on Display; Switch Window Source.\line
{\b Matrix Routing (recommended):} Select Source (Arm); Route to Display; Clear Source Selection. Tap a source once, then tap each destination.\line
{\b Matrix:} Route Source to Display (video+audio, immediate); Route Source to Multiple Displays (up to 3); Recall Matrix Preset.\line
{\b Video Wall:} Recall Wall Layout; Swap Wall Source (target a cell as Row:Col or All Cells).\line
{\b Media Player:} Apply Playlist to Display; Upload Playlist to Displays (up to 3).\line
{\b Device Control:} Reboot Device; Identify Light On/Off/Flash; OSD On/Off; Display Stream On/Off.\line
{\b System:} Reboot CBOX; Query Device List; Query All Device Status; Send Raw Command (advanced \'96 sends any API string verbatim).\line
{\b Live Lists:} Refresh Matrix Routes re-reads which source each display is tuned to.\line
{\b CEC:} Display Power On/Off (one device, up to 3 displays, or all displays); Send CEC Command (a stored C1-C16 payload to one device, up to 3 displays, or all displays); Send Raw CEC Data.\line
\line
{\b AUTOPROGRAMMING TAGS \line}
Tag template buttons with these and Integration Designer assigns the commands automatically. These tags are custom to this driver, so PackageDriver warns about them and stock RTI templates will not match \'96 your template must use the same names.\line
\tab {\b SelectInput1-SelectInput64} \'96 arm the matrix source in Input slot 1-64. The button's Reversed state lights while that source is armed.\line
\tab {\b RouteToDisplay1-RouteToDisplay64} \'96 route the armed source to Output slot 1-64. The destination follows the name and number entered in that Output (Display) Definition.\line
\tab {\b SelectWinID1-SelectWinID16} \'96 arm Multiview window 1-16. The button's Reversed state lights while that window is armed.\line
\tab {\b MVIDInput1-MVIDInput64} \'96 route Input slot 1-64 into the armed Multiview window.\line
\tab {\b InputName1-InputName64} \'96 puts the name entered in Input slot 1-64 on the button.\line
\tab {\b OutputName1-OutputName64} \'96 puts the name entered in Output slot 1-64 on the button.\line
\tab {\b OutputSource1-OutputSource64} \'96 puts the source currently feeding Output 1-64 on the button, updating live.\line
\tab {\b MVIDWinSource1-MVIDWinSource16} \'96 puts the source currently in Multiview window 1-16 on the button, updating live.\line
\tab {\b SelectedInputName} \'96 the armed source. {\b SelectedOutputName} \'96 the display picked in the Display List. {\b SelectedOutputSource} \'96 the source feeding that display. Useful for a "now routing X to Y" banner.\line
\tab A button can carry a command tag and a text tag together: tag it {\b SelectInput3} and {\b InputName3} and it arms input 3, lights while armed, and labels itself from the driver configuration.\line
\tab Tags for slots past the configured Number of Inputs / Number of Outputs are not offered, so the tag list follows the system size.\line
\line
{\b MATRIX ROUTING (SELECT SOURCE, THEN DESTINATIONS) \line}
Attach {\b Select Source (Arm)} to each source button and {\b Route to Display} to each destination button. The operator taps a source once, then taps TV 1, TV 3, TV 4 and each routes the armed source immediately \'96 one source tap, not one per destination. The source stays armed until another is tapped or {\b Clear Source Selection} runs.\line
\tab {\b Selected Source ID} is the armed input slot number. Each source also gets a boolean named after its {\b Input (Source) Definitions} slot (a source defined as TX-Apple appears as {\b TX-Apple Selected}); bind a source button's Reversed state to its own row to highlight it. Only sources covered by {\b Number of Inputs} are listed.\line
\tab A destination tap with no source armed does nothing, so a stray press cannot route.\line
\tab The armed source is shared with the live Source List, so a source armed either way works with both the slot destination buttons and {\b Select Display and Route} in the Live Lists. Arming from the list sets Selected Source ID to 0 (no slot highlighted).\line
\tab The original {\b Matrix} functions (Route Source to Display, and to Multiple Displays) still work for pages already built around them.\line
\line
{\b MATRIX ROUTE FEEDBACK \line}
Each output gets a variable named after its {\b Output (Display) Definitions} slot \'96 an output defined as RX-Bar appears as {\b RX-Bar Source} \'96 reporting the source currently feeding it, read back from the CBOX with {\i config get device routes}. Only outputs covered by {\b Number of Outputs} are listed, so the variable list matches the system instead of always showing 64 rows. {\b Selected Display Source} does the same for the display picked in the live Display List.\line
\tab The driver re-reads the routes automatically on connect, after Route Source to Display, Route Source to Multiple Displays, Recall Matrix Preset, and Route Selected Source to Display. {\b Refresh Matrix Routes} (Live Lists) re-reads them on demand.\line
\tab Names come from the device list, so run {\b Refresh Devices} (or connect) at least once or the variables show raw MAC addresses instead of TX names.\line
\tab A display with no route reports an empty string. These variables track video routing; audio breakaway is not surfaced.\line
\line
{\b CEC DETAIL \line}
CEC functions build {\i config set device cec \{hexData\} \{device\}}.\line
\tab {\b Power buttons} send the {\i poweron} / {\i poweroff} keywords. The CBOX then emits its own set of the most commonly used CEC power frames at the sink device from that endpoint \'96 use these rather than hand-building a power frame.\line
\tab {\b Stored payloads} (C1-C16) take CEC data as one space-free block, e.g. {\i 0036}. Send several frames in one command by separating them with commas, e.g. {\i 0036,0037}. A slot may also hold {\i poweron} or {\i poweroff}. Spaces you type are stripped, so {\i 0036, 0037} is sent as {\i 0036,0037}.\line
\tab {\b Targets} may be an encoder (TX) or a decoder (RX) \'96 CEC goes out of that endpoint to whatever is connected to it. Multi-display functions join endpoints with a colon; the API accepts fewer than 50 endpoints per command. The All Displays functions use ALLRX.\line
\tab The USP API has {\b no CEC query}, so the driver cannot read a display's real power state. Buttons are send-only.\line
\line
{\b SYSTEM VARIABLES \line}
{\b Status:} Connection Status (Connected/Disconnected).\line
{\b Command Feedback:} Last Command Success (OK/Error), Last Command Sent, Last Response (Raw), Last Response Command, Last Response Info, Last Response Code.\line
{\b Matrix Source Selected:} one boolean per configured input, named after its definition slot, for source-button highlighting.\line
{\b Matrix Routing (per Output):} one variable per configured output, named after its definition slot, reporting the source feeding it. Also Selected Display Source in Live Lists.\line
{\b CEC:} Last CEC Data, Last CEC Target \'96 what the driver last sent, not the sink's actual state.\line
{\b System State:} Active Matrix Name, Active Layout Name, Active Video Wall, Active Wall Layout, Device Status (Raw). Active-state variables update from the CBOX reply to an activate command; use Query All Device Status to refresh Device Status (Raw).\line
}
